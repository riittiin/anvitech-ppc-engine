[pytest]
markers =
    slow: real-subprocess / real-HTTP end-to-end tests (~1-2 min each); run as
        part of the default `pytest -q` full suite, not deselected — there is
        no prior slow-test convention in this repo to override.
